const optionalWords = ['Phillipines', 'America', 'Great Britain', 'Antartica', 'South Africa'];

let chosenWord = optionalWords.randomn;
var emptyBucket = []; 

emptyBucket.push((chosenWord.length) * "_");

var guessedWord;

var wordDecipher = function(input, emptyBucket){
  input = input.split("");
  input.forEach(function(element)){
    
  }
}

